<%= form_for(@appointment_treatment) do |f| %>
  <% if @appointment_treatment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@appointment_treatment.errors.count, "error") %> prohibited this appointment_treatment from being saved:</h2>

      <ul>
      <% @appointment_treatment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tooth_treatment_id %><br>
    <%= f.number_field :tooth_treatment_id %>
  </div>
  <div class="field">
    <%= f.label :appointment_id %><br>
    <%= f.number_field :appointment_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
